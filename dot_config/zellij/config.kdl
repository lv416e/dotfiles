// =============================================================================
// Zellij Configuration - Custom tmux-compatible keybindings
// =============================================================================
// Docs: https://zellij.dev/documentation/
//
// This configuration matches the custom tmux config in dot_tmux.conf.
// Key customizations from standard tmux:
//   - Uses Ctrl+g prefix (custom, not standard Ctrl+b)
//   - Split panes: \ (horizontal) and - (vertical)
//   - Pane resize: Ctrl+h/j/k/l (repeatable in tmux)
//   - Vi-mode copy bindings: v (selection), y (copy)
//   - Jump key: / for search
//   - Simplified UI for minimal distraction

// =============================================================================
// Keybindings - tmux-compatible mode
// =============================================================================
keybinds {
    // Normal mode: Only Ctrl+g to enter tmux mode (matching custom tmux config)
    // This prevents conflicts with Neovim and other CLI tools
    normal clear-defaults=true {
        bind "Ctrl g" { SwitchToMode "tmux"; }
        bind "Alt n" { NewPane; }
        bind "Alt h" { MoveFocusOrTab "Left"; }
        bind "Alt l" { MoveFocusOrTab "Right"; }
        bind "Alt j" { MoveFocus "Down"; }
        bind "Alt k" { MoveFocus "Up"; }
        bind "Alt =" { Resize "Increase"; }
        bind "Alt -" { Resize "Decrease"; }
    }

    // Tmux mode: Custom tmux keybindings (matching dot_tmux.conf)
    tmux clear-defaults=true {
        // Mode switching
        bind "Esc" { SwitchToMode "Normal"; }
        bind "Ctrl g" { Write 2; SwitchToMode "Normal"; }  // Pass through Ctrl+g
        bind "g" { SwitchToMode "Locked"; }
        bind "[" { SwitchToMode "Scroll"; }

        // Pane management (custom tmux-style)
        bind "c" { NewPane; SwitchToMode "Normal"; }                    // New pane (like tmux's new window)
        bind "\\" { NewPane "Right"; SwitchToMode "Normal"; }           // Split horizontal (custom: \ instead of ")
        bind "-" { NewPane "Down"; SwitchToMode "Normal"; }             // Split vertical (custom: - instead of %)
        bind "x" { CloseFocus; SwitchToMode "Normal"; }                 // Close pane
        bind "z" { ToggleFocusFullscreen; SwitchToMode "Normal"; }      // Zoom pane

        // Pane navigation (h/j/k/l)
        bind "h" { MoveFocus "Left"; }
        bind "j" { MoveFocus "Down"; }
        bind "k" { MoveFocus "Up"; }
        bind "l" { MoveFocus "Right"; }
        bind "Left" { MoveFocus "Left"; }
        bind "Down" { MoveFocus "Down"; }
        bind "Up" { MoveFocus "Up"; }
        bind "Right" { MoveFocus "Right"; }

        // Tab management (like tmux windows)
        bind "t" { NewTab; SwitchToMode "Normal"; }                     // New tab (like tmux window)
        bind "&" { CloseTab; SwitchToMode "Normal"; }                   // Close tab
        bind "n" { GoToNextTab; SwitchToMode "Normal"; }                // Next tab
        bind "p" { GoToPreviousTab; SwitchToMode "Normal"; }            // Previous tab
        bind "1" { GoToTab 1; SwitchToMode "Normal"; }
        bind "2" { GoToTab 2; SwitchToMode "Normal"; }
        bind "3" { GoToTab 3; SwitchToMode "Normal"; }
        bind "4" { GoToTab 4; SwitchToMode "Normal"; }
        bind "5" { GoToTab 5; SwitchToMode "Normal"; }
        bind "6" { GoToTab 6; SwitchToMode "Normal"; }
        bind "7" { GoToTab 7; SwitchToMode "Normal"; }
        bind "8" { GoToTab 8; SwitchToMode "Normal"; }
        bind "9" { GoToTab 9; SwitchToMode "Normal"; }
        bind "," { SwitchToMode "RenameTab"; TabNameInput 0; }          // Rename tab

        // Session management
        bind "d" { Detach; }                                             // Detach from session
        bind "w" { SwitchToMode "Session"; }                            // Session menu

        // Pane resizing (custom tmux style: Ctrl+h/j/k/l in resize mode)
        bind "Ctrl h" { Resize "Left"; }
        bind "Ctrl j" { Resize "Down"; }
        bind "Ctrl k" { Resize "Up"; }
        bind "Ctrl l" { Resize "Right"; }

        // Resize mode (for more control)
        bind "r" { SwitchToMode "Resize"; }

        // Pane mode (for swapping, moving)
        bind "s" { SwitchToMode "Pane"; }

        // Search mode (custom tmux: / for jump)
        bind "/" { SwitchToMode "EnterSearch"; SearchInput 0; }

        // Manual restore (custom tmux: Ctrl+r for tmux-resurrect)
        // Note: Zellij doesn't have built-in session restore like tmux-resurrect
        // This keybinding is reserved for future session management plugin
        bind "Ctrl r" { SwitchToMode "Session"; }

        // Help
        bind "?" { SwitchToMode "Tmux"; }  // Show this menu again
        bind "q" { Quit; }
    }

    // Locked mode: Disable all zellij keybindings (useful when nested)
    locked {
        bind "Ctrl b" "g" { SwitchToMode "Normal"; }
    }

    // Resize mode: Adjust pane sizes
    resize {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "h" "Left" { Resize "Increase Left"; }
        bind "j" "Down" { Resize "Increase Down"; }
        bind "k" "Up" { Resize "Increase Up"; }
        bind "l" "Right" { Resize "Increase Right"; }
        bind "H" { Resize "Decrease Left"; }
        bind "J" { Resize "Decrease Down"; }
        bind "K" { Resize "Decrease Up"; }
        bind "L" { Resize "Decrease Right"; }
        bind "=" { Resize "Increase"; }
        bind "-" { Resize "Decrease"; }
    }

    // Pane mode: Swap and move panes
    pane {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "h" "Left" { MoveFocus "Left"; }
        bind "j" "Down" { MoveFocus "Down"; }
        bind "k" "Up" { MoveFocus "Up"; }
        bind "l" "Right" { MoveFocus "Right"; }
        bind "f" { ToggleFocusFullscreen; SwitchToMode "Normal"; }
        bind "z" { TogglePaneFrames; SwitchToMode "Normal"; }
        bind "w" { ToggleFloatingPanes; SwitchToMode "Normal"; }
        bind "e" { TogglePaneEmbedOrFloating; SwitchToMode "Normal"; }
        bind "n" { NewPane; SwitchToMode "Normal"; }
        bind "x" { CloseFocus; SwitchToMode "Normal"; }
    }

    // Session mode: Manage sessions
    session {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "d" { Detach; }
        bind "w" {
            LaunchOrFocusPlugin "session-manager" {
                floating true
            }
            SwitchToMode "Normal"
        }
    }

    // Scroll mode: Scroll through terminal history (like tmux copy mode with vi bindings)
    scroll {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "e" { EditScrollback; SwitchToMode "Normal"; }
        bind "/" { SwitchToMode "EnterSearch"; SearchInput 0; }
        bind "Ctrl c" { ScrollToBottom; SwitchToMode "Normal"; }
        bind "j" "Down" { ScrollDown; }
        bind "k" "Up" { ScrollUp; }
        bind "Ctrl f" "PageDown" "Right" "l" { PageScrollDown; }
        bind "Ctrl b" "PageUp" "Left" "h" { PageScrollUp; }
        bind "d" { HalfPageScrollDown; }
        bind "u" { HalfPageScrollUp; }
        // Vi-mode copy bindings (custom tmux: v for selection, y for copy)
        // Note: Zellij uses 'Space' for selection start, 'Enter' for copy
        // But we add 'v' and 'y' for tmux vi-mode compatibility
        bind "Space" "v" { SwitchToMode "Normal"; }  // Begin selection (vi-mode)
    }

    // Search mode: Search in scrollback
    search {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "Ctrl c" { ScrollToBottom; SwitchToMode "Normal"; }
        bind "j" "Down" { ScrollDown; }
        bind "k" "Up" { ScrollUp; }
        bind "Ctrl f" "PageDown" { PageScrollDown; }
        bind "Ctrl b" "PageUp" { PageScrollUp; }
        bind "d" { HalfPageScrollDown; }
        bind "u" { HalfPageScrollUp; }
        bind "n" { Search "down"; }
        bind "N" { Search "up"; }
        bind "c" { SearchToggleOption "CaseSensitivity"; }
        bind "w" { SearchToggleOption "Wrap"; }
        bind "o" { SearchToggleOption "WholeWord"; }
    }

    // Tab rename mode
    renametab {
        bind "Esc" { SwitchToMode "Normal"; }
    }

    // Pane rename mode
    renamepane {
        bind "Esc" { SwitchToMode "Normal"; }
    }
}

// =============================================================================
// UI Settings
// =============================================================================
// Simplified UI for minimal distraction (more tmux-like)
simplified_ui true

// Pane frames
pane_frames false

// Default layout
// Options: default, compact, with-status
default_layout "compact"

// =============================================================================
// Theme
// =============================================================================
// Using a dark theme similar to tmux
// Available themes: default, dracula, gruvbox, nord, catppuccin-*
// You can also define custom colors below
theme "default"

// Custom theme (optional - uncomment to use)
// themes {
//     default {
//         fg "#D8DEE9"
//         bg "#2E3440"
//         black "#3B4252"
//         red "#BF616A"
//         green "#A3BE8C"
//         yellow "#EBCB8B"
//         blue "#81A1C1"
//         magenta "#B48EAD"
//         cyan "#88C0D0"
//         white "#E5E9F0"
//         orange "#D08770"
//     }
// }

// =============================================================================
// Behavior
// =============================================================================
// Automatically attach to existing session or create new one
default_mode "normal"

// Copy to clipboard
copy_command "pbcopy"  // macOS clipboard

// Scroll buffer size (number of lines)
scroll_buffer_size 10000

// Mouse mode
mouse_mode true

// =============================================================================
// Session
// =============================================================================
// Session name template
// session_name "zellij"

// Auto-attach to a session on startup
// auto_attach_to_sessions true

// Serialize pane viewport on exit
// on_force_close "quit"
